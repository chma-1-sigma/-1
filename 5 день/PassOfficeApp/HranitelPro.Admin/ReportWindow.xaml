<Window x:Class="HranitelPro.Admin.ReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Отчеты" Height="600" Width="800"
        WindowStartupLocation="CenterOwner" Background="#F0F4F8">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Отчеты о посещениях" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="15" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Дата начала" Margin="0,0,0,5"/>
                    <DatePicker x:Name="startDatePicker" Height="35"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,10,0">
                    <TextBlock Text="Дата окончания" Margin="0,0,0,5"/>
                    <DatePicker x:Name="endDatePicker" Height="35"/>
                </StackPanel>

                <Button Grid.Column="2" Content="Сформировать" Click="GenerateReport_Click" 
                        Background="#3498DB" Foreground="White" Height="35" Padding="20,0" Cursor="Hand" VerticalAlignment="Bottom"/>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2" x:Name="reportGrid" AutoGenerateColumns="False" IsReadOnly="True" 
                  HeadersVisibility="Column" BorderThickness="0" RowHeight="35" 
                  AlternatingRowBackground="#F8F9FA" FontSize="13">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата" Binding="{Binding Date, StringFormat=dd.MM.yyyy}" Width="120"/>
                <DataGridTextColumn Header="Подразделение" Binding="{Binding DepartmentName}" Width="200"/>
                <DataGridTextColumn Header="Количество посещений" Binding="{Binding VisitCount}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button x:Name="btnExport" Content="Экспорт в Excel" Click="ExportToExcel_Click" 
                    Background="#27AE60" Foreground="White" Height="40" Width="150" Cursor="Hand" Margin="0,0,10,0"/>
            <Button x:Name="btnClose" Content="Закрыть" Click="Close_Click" 
                    Background="#95A5A6" Foreground="White" Height="40" Width="150" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>